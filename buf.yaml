version: v2

modules:
  - path: contracts/src/main/proto

lint:
  use:
    - STANDARD

breaking:
  use:
    # FILE is the strictest useful level: it catches field renumbering, type
    # changes and deletions - the changes that silently corrupt data already
    # sitting in a Kafka topic.
    - FILE
